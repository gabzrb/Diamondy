<div class="container">
  <div class="row">
    <div class=" col col-xs-12 col-lg-12">
      <div class="contactcard text-center">
        <h3>Montre⌚️</h3>
        <p>Page 2/2</p>
        <%= simple_form_for(@watch, as: :watch, method: :post, url: product_watchs_path(@product)) do |f| %>
        <%= f.error_notification %>
        <div class="form-row">
          <div class="col-xs-12 col-md-6">
            <%= f.input :state, collection: Watch::STATES, label:'État' %>
          </div>
          <div class="col-xs-12 col-md-6">
            <%= f.input :brand, collection: Watch::BRANDS, label:'Marque' %>
          </div>
          <div class="col-xs-12 col-md-6">
            <%= f.input :model, label:'Modèle' %>
          </div>
          <div class="col-xs-12 col-md-6">
            <%= f.input :year, min:1800 , max: 2020, label:'Année' %>
          </div>
          <div class="col-xs-12 col-md-12">
            <%= f.input :material, collection: Watch::MATERIALS, label:'Matériaux' %>
          </div>
          <div class="form-actions col col-xs-12 col-md-6">
            <%= f.submit "Valider", class: "btn-contact" %>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
