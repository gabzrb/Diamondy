<div class="container list-nav">
<%= form_tag jewels_path, method: :get do %>
<%= select_tag 'query[category]', options_for_select(Jewel::CATEGORY) %>
<%= select_tag 'query[state]', options_for_select(Jewel::STATES) %>
<%= select_tag 'query[brand]', options_for_select(Jewel::CATEGORY) %>
<%= submit_tag "Search" %>
<%= link_to "🅧", jewels_path %>
<% end %>
  <% @jewels.each do |jewel| %>
  <div class="line_confirmation"></div>
  <div class="card-trip">
    <% if jewel.product.product_attachments.first %>
       <img src= <%= jewel.product.product_attachments.first.photo.url %> >
   <% else %>
      <img src="https://i.ibb.co/NNZJd2h/serkan-turk-v6jc-Ubxet-L0-unsplash.jpg" >
    <% end %>
    <div class="card-trip-infos">
      <div>
        <h2> <%= jewel.category %></h2>
        <p><%= jewel.product.description %></p>
      </div>
      <div >
        <%= link_to 'Voir Produit',jewel_path(jewel), class: "btn btn-contact" %>
      </div>
      <h2 class="card-trip-pricing"> <%= jewel.product.price.to_i %>€</h2>
      <% if jewel.product.user.photo.url %>
      <%= cl_image_tag jewel.product.user.photo, class:"card-trip-user avatar-bordered"  %>
      <% else %>
      <img src="https://i.ibb.co/mhL9zjd/user.png" class="card-trip-user avatar-bordered">
      <% end %>
    </div>
  </div>
  <% end %>
</div>
